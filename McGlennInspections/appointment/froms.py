from django import forms


class AppointmentForm(forms.Form):
    ''' AppointmentForm:  Class used to create the basic appointment
    form that the user inputs thier request.
    '''
    full_name =
    email =
    phone =
    inspection_address =
    date_requested =
    time_requested =
    notes =
